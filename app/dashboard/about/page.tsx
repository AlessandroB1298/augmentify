import PageServer from "../(overview)/PageServer"
export default function Page(){
    return(
        <div className={"flex justify-center items-center min-h-screen sm:justify-left flex-col gap-0"}>
            <PageServer/>
        </div>
    )
}